// Date is a type already defined in TS
const today = new Date();
today.getMonth();

// person is now a new object defined in this project
const person = {
  age: 20,
};
person.age;

// Color is a new class also defined in this project
class Color {}
const red = new Color();
